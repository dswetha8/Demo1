name: Demo1

on: 
  schedule:
    - cron: "*/1 * * * *"

jobs:
  init:
    runs-on: windows-latest
    steps:
      - name: Run a one-line script
        run: Echo "HelloWorld!!First script is sucess!!!"
